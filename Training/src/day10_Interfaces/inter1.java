package day10_Interfaces;

//interfaces can implement another interface!
public interface inter1 {
	
	//not instance variables - only CONSTANT variables
	//can have final variables -this happens by default! they are static and final
	//must have a value and must be initialized to a value
	int i=0;
	
	void display();
	int area();
	
	
}
